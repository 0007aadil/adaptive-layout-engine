/**
 * Public entry point for the DOM renderer, matching the assignment's named
 * structure (`src/render-dom.ts`). The component lives in
 * `src/components/AdFrame.tsx` alongside the app's other UI primitives, so it
 * can be reused by the surface grid and the inspector without duplication.
 */
export { AdFrame } from './components/AdFrame'
